// The mock API requires this header on every request:
//   x-access-token: <API_TOKEN>
// Hardcoding it here is fine for the exercise.
export const API_TOKEN = 'interview-token-2026';
export const API_BASE = '/api';
